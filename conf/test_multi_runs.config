params {
    sc {
        scenic {
            container = "docker://dweemx/sctx-pyscenic:0.9.19-1"
            numRuns = 2
            // AUCell parameters
            aucell {
                // only used when running in multi-runs mode
                // percentile_threshold = 0.01 // will override auc_threshold if uncommented
                min_genes_regulon = 1
                min_regulon_gene_occurrence = 1
            }
        }
    }
}
