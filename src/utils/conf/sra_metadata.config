params {
    global {
        outdir = 'out'
    }
    utils {
        container = 'vibsinglecellnf/utils:0.2.1'
        sra_metadata {
            mode = 'web' // or db
            // sraDb = ''
            // sraDbForceDownload = false
            // sraDbOutDir = ''
        }
    }
}
