params {
    global {
        outdir = 'out'
    }
    utils {
        container = 'vibsinglecellnf/utils:0.3.0'
        sra_metadata {
            mode = 'web' // or db
            // sraDb = ''
            // sraDbForceDownload = false
            // sraDbOutDir = ''
        }
    }
}
