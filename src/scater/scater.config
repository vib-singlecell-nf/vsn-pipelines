params {
    sc {
        scater {
            container = 'qrouchon/r-filtering:latest'
            filter {
                nmads = 3
                iff = "10x_mtx"
                off = "rds"
                outdir = "out"
            }
        }
    }
}